[package]
name = "surface-saver-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
serde_json = "1.0"
jsonschema = "0.18"

[dev-dependencies]
tempfile = "3.10"
